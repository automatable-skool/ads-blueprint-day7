Card is the default container: white, hairline border, short warm shadow.

```jsx
<Card padding="lg" elevation="sm"><h3>Emergency call-outs</h3></Card>
```

`accent` adds the amber 3px top rule — use it on exactly one card in a set (the recommended package). Pass `as="a"` with `href` for a fully clickable card.
