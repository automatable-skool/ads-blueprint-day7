IconButton is a square icon-only control for toolbars, dialog closes and compact rows. `label` is required.

```jsx
<IconButton name="menu" label="Open menu" variant="ghost" />
```

Variants secondary / ghost / brand; sizes sm (30px) / md (38px) / lg (46px). Wrap in Tooltip when the glyph is not obvious.
