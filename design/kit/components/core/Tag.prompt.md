Tag is a sentence-case bordered chip for filters, service areas and chosen options.

```jsx
<Tag icon="map-pin" onRemove={() => remove("Burlington")}>Burlington</Tag>
```

Unlike Badge it is sentence case, bordered rather than filled, and may be removable.
