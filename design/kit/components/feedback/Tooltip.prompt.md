Tooltip labels icon-only controls on hover and focus.

```jsx
<Tooltip label="Print work order"><IconButton name="file-text" label="Print work order" /></Tooltip>
```

A few words, never essential information — placement top / bottom / right.
