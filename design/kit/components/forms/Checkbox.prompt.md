Checkbox is for form values and multi-select lists.

```jsx
<Checkbox label="Text me when the tech is on the way" defaultChecked />
```

Controlled via `checked`+`onChange(next)`, otherwise `defaultChecked`. For a setting that applies instantly, use Switch.
