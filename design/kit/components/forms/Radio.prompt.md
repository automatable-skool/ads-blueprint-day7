Radio/RadioGroup handle single choice — time windows, urgency, contact preference.

```jsx
<RadioGroup name="window" value={w} onChange={setW} options={["Morning (8–12)", "Afternoon (12–5)"]} />
```

`direction="horizontal"` for two short options. Always give the group a Field label.
