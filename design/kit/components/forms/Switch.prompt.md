Switch toggles something that takes effect immediately.

```jsx
<Switch label="Accepting new jobs" defaultChecked />
```

Never use it inside a form that has a Save button — that is Checkbox territory.
