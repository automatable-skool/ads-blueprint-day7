PromoBar is the accent strip above the header — one offer, one link, never two.

```jsx
<PromoBar message="Furnace or AC trouble? Upfront pricing in 60 seconds." ctaLabel="Get my estimate" />
```

It is the only place yellow covers a full-width area. Remove it rather than leaving a stale promotion up.
