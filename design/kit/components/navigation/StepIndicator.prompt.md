StepIndicator shows position in a quote or booking flow.

```jsx
<StepIndicator steps={["Job", "Details", "Time", "Confirm"]} current={1} />
```

Completed steps are green with a check, the current step is amber, later steps are outlined. Three to five steps.
