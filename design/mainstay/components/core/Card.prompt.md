The generic surface container — white, hairline border, 14px radius.

```jsx
<Card variant="elevated" padding="lg">Anything.</Card>
```

Only one elevation level per screen region; don't nest elevated cards.
