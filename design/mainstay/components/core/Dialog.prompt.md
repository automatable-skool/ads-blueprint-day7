Modal overlay for booking confirmation, callback requests and short forms.

```jsx
<Dialog open={open} onClose={close} title="Request a callback" footer={<Button>Send</Button>}>…</Dialog>
```

Escape and scrim click both close. Scrim is ink at 42% with a 2px blur.
