A square, icon-only action — menu toggles, dismiss, carousel arrows.

```jsx
<IconButton icon="menu" label="Open menu" variant="outline" />
```

Never ship one without `label`. Use `sm` only inside dense chrome (dialog close, toast dismiss).
