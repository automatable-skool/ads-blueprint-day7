Horizontal view switcher — underline for page sections, pill for compact filters.

```jsx
<Tabs items={[{id:"res",label:"Residential"},{id:"com",label:"Commercial"}]} onChange={setTab} />
```

Active tab is marked by a 2px clay underline, never by a colour fill.
