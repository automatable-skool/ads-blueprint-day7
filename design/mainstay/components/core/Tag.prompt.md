Interactive chip for filtering service categories or showing removable selections.

```jsx
<Tag icon="droplets" selected onSelect={() => setFilter("plumbing")}>Plumbing</Tag>
```

Selected state uses the accent-50 / accent-300 pair, never a solid fill.
