Transient confirmation or error notice, typically after a form submit.

```jsx
<Toast tone="success" title="Quote requested" message="We'll call you within the hour." onDismiss={hide} />
```

Positioning is the app's job; the component is a plain block.
