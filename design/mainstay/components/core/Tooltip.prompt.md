Hover/focus hint on an inline element.

```jsx
<Tooltip content="Includes parts and labour"><Icon name="info" size={16} /></Tooltip>
```

Ink bubble, 13px text, max 220px. Never put actions inside a tooltip.
