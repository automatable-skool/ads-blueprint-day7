Single-line text field with label, hint and error states.

```jsx
<Input label="Phone" type="tel" icon="phone" required hint="We only use this to confirm your slot." />
```

Labels are always visible — no placeholder-only fields in this system.
