Radio input; `card` turns it into a selectable panel for booking options.

```jsx
<Radio card name="slot" label="Morning" description="8am – 12pm" />
```

Card variant tints to accent-50 when selected.
