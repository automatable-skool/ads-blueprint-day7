Native select with the system's control styling and a chevron affordance.

```jsx
<Select label="Service" placeholder="Choose a service" options={["Emergency repair","Installation"]} />
```

Native element on purpose — it is the most reliable control on mobile.
