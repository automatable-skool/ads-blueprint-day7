Fixed-price package card for service tiers or callout rates.

```jsx
<PricingCard featured badge="Most booked" name="Standard callout" amount="$149" unit="/ visit" features={["Same-day slot","Parts under $25 included"]} />
```

Feature at most one card per row.
