The system's conversion centrepiece — a short lead-capture form with a built-in success state.

```jsx
<QuoteForm services={["Emergency repair","Installation"]} onSubmit={send} />
```

Five fields maximum; name and phone are the only required ones. Submitting swaps the form for the confirmation panel.
