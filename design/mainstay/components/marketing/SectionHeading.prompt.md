Eyebrow + heading + lead trio that opens every marketing section.

```jsx
<SectionHeading eyebrow="What we do" title="Trades and services, one number to call" lead="…" />
```

Keep the lead under two lines; max-width is capped at 640px.
