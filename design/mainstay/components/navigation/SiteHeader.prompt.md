Sticky site header: ink contact strip, wordmark, nav, phone number and CTA.

```jsx
<SiteHeader brand="Mainstay" activeHref="#services" onNavigate={go} />
```

Phone number stays visible at every breakpoint; the nav collapses to a burger under 900px.
